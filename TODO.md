# TODO — zkm-pdf

This is a stub. The broader work ledger is the central zkm `TODO.md`
(`~/src/zkm/TODO.md`); executor-facing items for THIS repo live in
[ROADMAP.md](ROADMAP.md), which is the single source of truth.

- [ ] Relay: 1 open ROADMAP item — id:9475 adopt shared `zkm.pdftext` helper + density pilot [HARD — pool]. Gate LIFTED 2026-06-24: the D2 unification is decided and the `zkm.pdftext` helper (`probe`/`is_scanned_only`/`resolve_threshold`) was BUILT 2026-06-22, so 9475 re-scoped [HARD — meeting]→[HARD — pool] — only the plugin-side adoption never landed (zkm-pdf still uses its local min-text rule, no `zkm.pdftext` import). Executor-ready; still coordinated with zkm-scan id:02bd (shared `pdf_text_threshold` key, no isolated recalibration). id:1a30 (eml password-token boundary) closed 2026-06-17. <!-- id:32fe -->
